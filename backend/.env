# PostgreSQL
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pharmacy_db

# Security
SECRET_KEY=your-secret-key-here-make-it-long-and-random
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AWS Credentials (for development)
AWS_ACCESS_KEY_ID=dummy-access-key
AWS_SECRET_ACCESS_KEY=dummy-secret-key
AWS_REGION=us-east-1
S3_BUCKET=pharmacy-bucket

# API Settings
API_V1_STR=/api/v1
PROJECT_NAME=Pharmacy Management System

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# Redis (optional, using defaults if not set)
REDIS_HOST=localhost
REDIS_PORT=6379

# DynamoDB (optional, using default if not set)
DYNAMODB_TABLE=pms-medicines